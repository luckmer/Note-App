function NoteData({ item, OpenById }) {
  return <div></div>;
}

export default NoteData;

// {
/* <_.Div key={i}>
{drop}
<_.P key={item.id} onClick={() => OpenById(item.id)}>
  {item.name}
</_.P>
</_.Div> */
// }
